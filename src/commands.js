const add = require("./commands/add");
const list = require("./commands/list");
const del = require("./commands/delete");
const init = require("./commands/init");

module.exports = { add, list, del, init };
